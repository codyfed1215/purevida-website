---
// Landing Page: QR Code Flyer Campaign – Tap-Tap-Done Micro Funnel
// Standalone page — no shared Layout, no nav, no footer links
// Route: /lp/flyer
import '../../styles/global.css'

const SUPABASE_URL = 'https://tpqipscwaovlujxolfal.supabase.co';
---

<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Get Your Custom Quote | PureVida Water</title>
  <meta name="description" content="See what purified water costs for your office. Free installation included." />
  <meta name="robots" content="noindex, nofollow" />
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <link rel="icon" type="image/x-icon" href="/favicon.ico" />

  <!-- Google Analytics 4 -->
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-RT2GYZLRSW"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', 'G-RT2GYZLRSW');
  </script>

  <!-- Google Tag Manager -->
  <script>(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
  new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
  j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
  'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
  })(window,document,'script','dataLayer','GTM-NHL3XGV2');</script>

  <style is:global>
    .step { transition: opacity 0.3s ease, transform 0.3s ease; }
    .step.hidden { opacity: 0; transform: translateY(12px); pointer-events: none; position: absolute; }
    .step.visible { opacity: 1; transform: translateY(0); position: relative; }
    .size-btn, .solution-btn { transition: all 0.15s ease; }
    .size-btn:active, .solution-btn:active { transform: scale(0.97); }
    .price-reveal { animation: priceIn 0.4s ease-out; }
    @keyframes priceIn {
      from { opacity: 0; transform: scale(0.85); }
      to { opacity: 1; transform: scale(1); }
    }
  </style>
</head>

<body class="min-h-screen bg-white text-gray-900 antialiased">
  <!-- Google Tag Manager (noscript) -->
  <noscript><iframe src="https://www.googletagmanager.com/ns.html?id=GTM-NHL3XGV2" height="0" width="0" style="display:none;visibility:hidden"></iframe></noscript>

  <!-- ============ SLIM HEADER ============ -->
  <header class="sticky top-0 z-50 bg-white/95 backdrop-blur-md border-b border-gray-200">
    <div class="max-w-lg mx-auto px-4 py-3 flex items-center justify-between">
      <a href="https://purevidawater.com" class="flex items-center gap-2 shrink-0">
        <img src="/images/water-drop-logo.png" alt="PureVida" class="h-8 w-auto" />
        <span class="text-lg font-bold tracking-tight">
          <span class="text-sky-500">Pure</span><span class="text-sky-800">Vida</span>
        </span>
      </a>
      <a href="tel:+18885340117" class="flex items-center gap-1.5 text-gray-600 hover:text-sky-500 transition-colors text-sm font-medium">
        <svg class="w-4 h-4 shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/></svg>
        <span>(888) 534-0117</span>
      </a>
    </div>
  </header>

  <!-- ============ MICRO FUNNEL ============ -->
  <main class="max-w-lg mx-auto px-4 py-8 min-h-[70vh] flex flex-col justify-center">

    <!-- ---- Step 1: Office Size ---- -->
    <div id="step-1" class="step visible">
      <div class="text-center mb-6">
        <h1 class="text-2xl font-bold text-gray-900">Get Your Custom Quote</h1>
        <p class="mt-2 text-gray-500">How many people are in your office?</p>
      </div>
      <div class="grid grid-cols-2 gap-3">
        <button data-size="1-10" class="size-btn bg-white border-2 border-gray-200 rounded-2xl py-6 text-center cursor-pointer hover:border-sky-400 active:border-sky-500">
          <span class="block text-2xl font-bold text-gray-900">1–10</span>
          <span class="block text-sm text-gray-500 mt-1">people</span>
        </button>
        <button data-size="11-25" class="size-btn bg-white border-2 border-gray-200 rounded-2xl py-6 text-center cursor-pointer hover:border-sky-400 active:border-sky-500">
          <span class="block text-2xl font-bold text-gray-900">11–25</span>
          <span class="block text-sm text-gray-500 mt-1">people</span>
        </button>
        <button data-size="26-50" class="size-btn bg-white border-2 border-gray-200 rounded-2xl py-6 text-center cursor-pointer hover:border-sky-400 active:border-sky-500">
          <span class="block text-2xl font-bold text-gray-900">26–50</span>
          <span class="block text-sm text-gray-500 mt-1">people</span>
        </button>
        <button data-size="50+" class="size-btn bg-white border-2 border-gray-200 rounded-2xl py-6 text-center cursor-pointer hover:border-sky-400 active:border-sky-500">
          <span class="block text-2xl font-bold text-gray-900">50+</span>
          <span class="block text-sm text-gray-500 mt-1">people</span>
        </button>
      </div>
    </div>

    <!-- ---- Step 2: Current Solution ---- -->
    <div id="step-2" class="step hidden">
      <div class="text-center mb-6">
        <h1 class="text-2xl font-bold text-gray-900">What do you use now?</h1>
        <p class="mt-2 text-gray-500">For drinking water in your office</p>
      </div>
      <div class="grid grid-cols-3 gap-3">
        <button data-solution="Jugs" class="solution-btn bg-white border-2 border-gray-200 rounded-2xl py-6 text-center cursor-pointer hover:border-sky-400 active:border-sky-500">
          <svg class="w-8 h-8 mx-auto text-gray-400 mb-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707"/></svg>
          <span class="block text-sm font-semibold text-gray-900">Jugs</span>
        </button>
        <button data-solution="Bottles" class="solution-btn bg-white border-2 border-gray-200 rounded-2xl py-6 text-center cursor-pointer hover:border-sky-400 active:border-sky-500">
          <svg class="w-8 h-8 mx-auto text-gray-400 mb-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M19.5 12c0 4.136-3.364 7.5-7.5 7.5S4.5 16.136 4.5 12 7.864 4.5 12 4.5s7.5 3.364 7.5 7.5z"/></svg>
          <span class="block text-sm font-semibold text-gray-900">Bottles</span>
        </button>
        <button data-solution="Unknown" class="solution-btn bg-white border-2 border-gray-200 rounded-2xl py-6 text-center cursor-pointer hover:border-sky-400 active:border-sky-500">
          <svg class="w-8 h-8 mx-auto text-gray-400 mb-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 6v6m0 0v6m0-6h6m-6 0H6"/></svg>
          <span class="block text-sm font-semibold text-gray-900">Nothing</span>
        </button>
      </div>
      <button id="back-to-1" class="mt-4 text-sm text-gray-400 hover:text-gray-600 cursor-pointer bg-transparent border-none mx-auto block">&larr; Back</button>
    </div>

    <!-- ---- Step 3: Price Reveal + Form ---- -->
    <div id="step-3" class="step hidden">
      <div class="text-center mb-6">
        <p class="text-sm font-medium text-sky-600 uppercase tracking-wide">Your estimated price</p>
        <p id="price-display" class="text-5xl font-bold text-gray-900 mt-2 price-reveal">$74/mo</p>
        <p id="savings-msg" class="text-sm text-gray-500 mt-2">Includes free installation &amp; maintenance</p>
      </div>

      <form id="lead-form" class="space-y-3">
        <input type="text" id="inp-name" required placeholder="Your name" class="w-full px-4 py-3.5 border border-gray-300 rounded-xl focus:ring-2 focus:ring-sky-500 focus:border-sky-500 outline-none text-base" />
        <input type="text" id="inp-business" required placeholder="Business name" class="w-full px-4 py-3.5 border border-gray-300 rounded-xl focus:ring-2 focus:ring-sky-500 focus:border-sky-500 outline-none text-base" />
        <input type="tel" id="inp-phone" required placeholder="Phone number" class="w-full px-4 py-3.5 border border-gray-300 rounded-xl focus:ring-2 focus:ring-sky-500 focus:border-sky-500 outline-none text-base" />
        <button type="submit" id="submit-btn" class="w-full bg-sky-500 hover:bg-sky-600 text-white font-semibold py-4 px-6 rounded-xl transition-colors text-lg cursor-pointer shadow-lg shadow-sky-500/25 disabled:opacity-50 disabled:cursor-not-allowed">
          Claim Free Install &rarr;
        </button>
      </form>
      <button id="back-to-2" class="mt-4 text-sm text-gray-400 hover:text-gray-600 cursor-pointer bg-transparent border-none mx-auto block">&larr; Back</button>
    </div>

    <!-- ---- Success State ---- -->
    <div id="step-success" class="step hidden text-center">
      <div class="w-20 h-20 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-5">
        <svg class="w-10 h-10 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/></svg>
      </div>
      <h2 class="text-2xl font-bold text-gray-900 mb-2">You're All Set!</h2>
      <p class="text-gray-600">We'll reach out within 1 business day to get you started.</p>
      <p class="text-sm text-gray-400 mt-4">Free installation &bull; No obligation</p>
    </div>

  </main>

  <!-- ============ TRUST BAR ============ -->
  <div id="trust-bar" class="border-t border-gray-100 bg-gray-50 py-6">
    <div class="max-w-lg mx-auto px-4">
      <div class="grid grid-cols-3 gap-4 text-center">
        <div>
          <p class="text-lg font-bold text-gray-900">1,100+</p>
          <p class="text-xs text-gray-500">Businesses Served</p>
        </div>
        <div>
          <p class="text-lg font-bold text-gray-900">30–80%</p>
          <p class="text-xs text-gray-500">Cost Savings</p>
        </div>
        <div>
          <p class="text-lg font-bold text-gray-900">99.9%</p>
          <p class="text-xs text-gray-500">Contaminants Removed</p>
        </div>
      </div>
    </div>
  </div>

  <!-- ============ MINIMAL FOOTER ============ -->
  <footer class="bg-gray-900 text-white py-6">
    <div class="max-w-lg mx-auto px-4 text-center">
      <p class="font-semibold mb-1">PureVida Water</p>
      <a href="tel:+18885340117" class="text-gray-400 hover:text-white transition-colors text-sm">(888) 534-0117</a>
      <p class="text-gray-500 text-xs mt-2">&copy; 2026 PureVida Water. All rights reserved.</p>
    </div>
  </footer>

  <!-- Toast Notification -->
  <div id="toast" class="fixed top-6 left-1/2 -translate-x-1/2 z-[200] hidden">
    <div id="toast-inner" class="flex items-center gap-3 px-6 py-4 rounded-xl shadow-2xl text-white font-medium transition-all duration-300 opacity-0 -translate-y-4 bg-green-600">
      <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/></svg>
      <span>You're all set! We'll be in touch.</span>
    </div>
  </div>

  <!-- ============ SCRIPTS ============ -->
  <script define:vars={{ SUPABASE_URL }}>
    var API = SUPABASE_URL + '/functions/v1';

    // ---- State ----
    var funnelState = {
      officeSize: null,
      currentSolution: null,
    };

    // ---- Pricing lookup (60-month rental grid prices) ----
    var PRICING = {
      '1-10':  { price: 54,  label: '$54/mo' },
      '11-25': { price: 74,  label: '$74/mo' },
      '26-50': { price: 159, label: '$159/mo' },
      '50+':   { price: 159, label: '$159/mo' },
    };

    var SAVINGS_MSG = {
      'Jugs':    'Save up to 80% vs jug delivery — free install included',
      'Bottles': 'Save up to 60% vs bottled water — free install included',
      'Unknown': 'Unlimited purified water — free install included',
    };

    // ---- Step transitions ----
    function showStep(id) {
      var steps = document.querySelectorAll('.step');
      for (var i = 0; i < steps.length; i++) {
        steps[i].classList.remove('visible');
        steps[i].classList.add('hidden');
      }
      var target = document.getElementById(id);
      // Small delay so the CSS transition fires
      setTimeout(function() {
        target.classList.remove('hidden');
        target.classList.add('visible');
      }, 50);
      window.scrollTo({ top: 0, behavior: 'smooth' });
    }

    // ---- Step 1: Office Size ----
    var sizeBtns = document.querySelectorAll('.size-btn');
    for (var i = 0; i < sizeBtns.length; i++) {
      sizeBtns[i].addEventListener('click', function() {
        funnelState.officeSize = this.getAttribute('data-size');
        // Highlight selected
        for (var j = 0; j < sizeBtns.length; j++) {
          sizeBtns[j].classList.remove('border-sky-500', 'bg-sky-50');
        }
        this.classList.add('border-sky-500', 'bg-sky-50');
        // Advance after brief highlight
        setTimeout(function() { showStep('step-2'); }, 200);
      });
    }

    // ---- Step 2: Current Solution ----
    var solBtns = document.querySelectorAll('.solution-btn');
    for (var i = 0; i < solBtns.length; i++) {
      solBtns[i].addEventListener('click', function() {
        funnelState.currentSolution = this.getAttribute('data-solution');
        // Highlight selected
        for (var j = 0; j < solBtns.length; j++) {
          solBtns[j].classList.remove('border-sky-500', 'bg-sky-50');
        }
        this.classList.add('border-sky-500', 'bg-sky-50');
        // Set price & savings message
        var p = PRICING[funnelState.officeSize] || PRICING['11-25'];
        document.getElementById('price-display').textContent = p.label;
        document.getElementById('savings-msg').textContent = SAVINGS_MSG[funnelState.currentSolution] || SAVINGS_MSG['Unknown'];
        // Advance
        setTimeout(function() { showStep('step-3'); }, 200);
      });
    }

    // ---- Back buttons ----
    document.getElementById('back-to-1').addEventListener('click', function() { showStep('step-1'); });
    document.getElementById('back-to-2').addEventListener('click', function() { showStep('step-2'); });

    // ---- Phone Formatting ----
    var phoneInput = document.getElementById('inp-phone');
    phoneInput.addEventListener('input', function(e) {
      var val = e.target.value.replace(/\D/g, '');
      if (val.length > 10) val = val.slice(0, 10);
      if (val.length >= 7) {
        val = '(' + val.slice(0, 3) + ') ' + val.slice(3, 6) + '-' + val.slice(6);
      } else if (val.length >= 4) {
        val = '(' + val.slice(0, 3) + ') ' + val.slice(3);
      } else if (val.length >= 1) {
        val = '(' + val;
      }
      e.target.value = val;
    });

    // ---- Pre-fill promo from URL ----
    var urlParams = new URLSearchParams(window.location.search);
    var urlSource = urlParams.get('source') || 'qr_scan';
    var urlPromo = urlParams.get('promo') || '';

    // ---- Form Submit → ingest-lead ----
    var form = document.getElementById('lead-form');
    var submitBtn = document.getElementById('submit-btn');
    var submitting = false;

    form.addEventListener('submit', async function(e) {
      e.preventDefault();
      if (submitting) return;
      submitting = true;
      submitBtn.disabled = true;
      submitBtn.textContent = 'Submitting...';

      var fullName = document.getElementById('inp-name').value.trim();
      var nameParts = fullName.split(/\s+/);
      var firstName = nameParts[0] || '';
      var lastName = nameParts.slice(1).join(' ') || '';

      var payload = {
        source: urlSource,
        form_type: 'flyer',
        business_name: document.getElementById('inp-business').value.trim(),
        phone: document.getElementById('inp-phone').value.trim() || null,
        first_name: firstName || null,
        last_name: lastName || null,
        current_solution: funnelState.currentSolution || null,
        promo_code: urlPromo || null,
      };

      try {
        var res = await fetch(API + '/ingest-lead', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(payload),
        });
        var json = await res.json();

        if (json.ok) {
          if (typeof gtag === 'function') {
            gtag('event', 'generate_lead', {
              event_category: 'form',
              event_label: 'flyer_lp',
              office_size: funnelState.officeSize,
              current_solution: funnelState.currentSolution,
            });
          }
          showStep('step-success');
          showToast();
        } else {
          alert(json.error || 'Something went wrong. Please try again.');
          submitting = false;
          submitBtn.disabled = false;
          submitBtn.innerHTML = 'Claim Free Install &rarr;';
        }
      } catch (err) {
        alert('Network error. Please check your connection and try again.');
        submitting = false;
        submitBtn.disabled = false;
        submitBtn.innerHTML = 'Claim Free Install &rarr;';
      }
    });

    // ---- Toast ----
    function showToast() {
      var toast = document.getElementById('toast');
      var inner = document.getElementById('toast-inner');
      if (!toast || !inner) return;
      toast.classList.remove('hidden');
      setTimeout(function() {
        inner.classList.remove('opacity-0', '-translate-y-4');
        inner.classList.add('opacity-100', 'translate-y-0');
      }, 10);
      setTimeout(function() {
        inner.classList.remove('opacity-100', 'translate-y-0');
        inner.classList.add('opacity-0', '-translate-y-4');
        setTimeout(function() { toast.classList.add('hidden'); }, 300);
      }, 4000);
    }
  </script>

</body>
</html>
